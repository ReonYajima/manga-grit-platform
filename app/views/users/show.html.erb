<!DOCTYPE html>
<html>
<head>
  <title>マイページ - <%= @user.display_name %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta charset="UTF-8">
  
  <!-- Bootstrap CSS（CDN） -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</head>
<body style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); min-height: 100vh;">
  <!-- ナビゲーションバー -->
  <nav class="navbar navbar-dark shadow-sm mb-0" style="background-color: rgba(255,255,255,0.1); backdrop-filter: blur(10px);">
    <div class="container">
      <%= link_to root_path, class: "navbar-brand d-flex align-items-center text-white text-decoration-none" do %>
        <i class="bi bi-book-fill me-2" style="font-size: 1.5rem;"></i>
        <span class="fw-bold">マンガグリットプラットフォーム</span>
      <% end %>
      <div class="d-flex align-items-center">
        <!-- ポイント表示 -->
        <%= link_to ranking_path, class: "text-decoration-none me-3" do %>
          <span class="badge text-dark" style="font-size: 1.2rem; background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);">
            <i class="bi bi-star-fill"></i>
            <%= @user.total_points %>pt
          </span>
        <% end %>
        
        <%= link_to root_path, class: "btn btn-outline-light btn-sm" do %>
          <i class="bi bi-house"></i> ホーム
        <% end %>
      </div>
    </div>
  </nav>

  <!-- ヘッダー部分 -->
  <div class="py-5" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
    <div class="container text-center">
      <div class="mb-3" style="font-size: 6rem;">
        <i class="bi bi-person-circle text-white"></i>
      </div>
      <h1 class="text-white fw-bold mb-2" style="font-size: 2.5rem;"><%= @user.display_name %></h1>
      <p class="text-white-50 mb-2" style="font-size: 1.2rem;">@<%= @user.username %></p>
      <small class="text-white-50">
        <i class="bi bi-calendar-check"></i>
        登録日: <%= @user.created_at.strftime("%Y年%m月%d日") %> (<%= @days_since_signup %>日目)
      </small>
    </div>
  </div>

  <div class="container" style="margin-top: -50px; padding-bottom: 80px;">
    <!-- 統計カード -->
    <div class="row g-3 mb-4">
      <div class="col-md-3">
        <div class="card border-0 shadow-lg text-center py-4 hover-stat-card" style="background: white;">
          <div class="card-body">
            <div class="mb-2" style="font-size: 3rem;">📝</div>
            <div class="display-4 fw-bold text-primary mb-2"><%= @total_posts %></div>
            <h6 class="text-muted mb-0">投稿数</h6>
          </div>
        </div>
      </div>

      <div class="col-md-3">
        <div class="card border-0 shadow-lg text-center py-4 hover-stat-card" style="background: white;">
          <div class="card-body">
            <div class="mb-2" style="font-size: 3rem;">💬</div>
            <div class="display-4 fw-bold text-success mb-2"><%= @total_comments_received %></div>
            <h6 class="text-muted mb-0">コメント数</h6>
          </div>
        </div>
      </div>

      <div class="col-md-3">
        <div class="card border-0 shadow-lg text-center py-4 hover-stat-card" style="background: white;">
          <div class="card-body">
            <div class="mb-2" style="font-size: 3rem;">❤️</div>
            <div class="display-4 fw-bold text-danger mb-2"><%= @total_likes_received %></div>
            <h6 class="text-muted mb-0">獲得いいね</h6>
          </div>
        </div>
      </div>

      <div class="col-md-3">
        <div class="card border-0 shadow-lg text-center py-4 hover-stat-card" style="background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);">
          <div class="card-body">
            <div class="mb-2" style="font-size: 3rem;">⭐</div>
            <div class="display-4 fw-bold text-white mb-2"><%= @user.total_points %></div>
            <h6 class="text-white mb-0">総合ポイント</h6>
            <small class="text-white">ランキング: <%= @user.points_rank %>位</small>
          </div>
        </div>
      </div>
    </div>

    <!-- タブナビゲーション -->
    <ul class="nav nav-pills nav-fill mb-4 shadow-sm bg-white rounded-3 p-2" id="myTab" role="tablist">
      <li class="nav-item" role="presentation">
        <button class="nav-link active rounded-3" id="posts-tab" data-bs-toggle="tab" data-bs-target="#posts" type="button" role="tab">
          <i class="bi bi-file-text-fill"></i> 投稿一覧
        </button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link rounded-3" id="points-tab" data-bs-toggle="tab" data-bs-target="#points" type="button" role="tab">
          <i class="bi bi-star-fill"></i> ポイント履歴
        </button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link rounded-3" id="missions-tab" data-bs-toggle="tab" data-bs-target="#missions" type="button" role="tab">
          <i class="bi bi-trophy-fill"></i> ミッション
        </button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link rounded-3" id="tags-tab" data-bs-toggle="tab" data-bs-target="#tags" type="button" role="tab">
          <i class="bi bi-tags-fill"></i> よく使うタグ
        </button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link rounded-3" id="liked-tab" data-bs-toggle="tab" data-bs-target="#liked" type="button" role="tab">
          <i class="bi bi-heart-fill"></i> いいねした投稿
        </button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link rounded-3" id="measurements-tab" data-bs-toggle="tab" data-bs-target="#measurements" type="button" role="tab">
          <i class="bi bi-clipboard2-check"></i> 測定
        </button>
      </li>
    </ul>

    <!-- タブコンテンツ -->
    <div class="tab-content" id="myTabContent">
      <!-- 投稿一覧タブ -->
      <div class="tab-pane fade show active" id="posts" role="tabpanel">
        <% if @posts.any? %>
          <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            <% @posts.each do |post| %>
              <div class="col">
                <%= link_to [post.genre, post], class: "text-decoration-none" do %>
                  <div class="card border-0 shadow-lg h-100 hover-card" style="background: white;">
                    <% if post.image.attached? %>
                      <%= image_tag url_for(post.image), 
                          class: "card-img-top", 
                          style: "height: 200px; object-fit: cover;" %>
                    <% else %>
                      <div class="card-img-top bg-light d-flex align-items-center justify-content-center" 
                           style="height: 200px;">
                        <span style="font-size: 4rem;">📚</span>
                      </div>
                    <% end %>

                    <div class="card-body">
                      <span class="badge bg-primary mb-2"><%= post.genre.name %></span>
                      <h5 class="card-title text-dark text-truncate"><%= post.manga_title %></h5>
                      <p class="card-text text-muted small">
                        <%= truncate(post.content, length: 80) %>
                      </p>

                      <% if post.tag_list.any? %>
                        <div class="mb-2">
                          <% post.tag_list.first(2).each do |tag| %>
                            <span class="badge bg-secondary">#<%= tag %></span>
                          <% end %>
                        </div>
                      <% end %>

                      <div class="d-flex justify-content-between align-items-center">
                        <small class="text-muted"><%= post.created_at.strftime("%Y/%m/%d") %></small>
                        <div>
                          <span class="badge bg-danger">❤️ <%= post.likes_count %></span>
                          <span class="badge bg-success">💬 <%= post.comments.count %></span>
                        </div>
                      </div>
                    </div>
                  </div>
                <% end %>
              </div>
            <% end %>
          </div>

          <!-- ページネーション -->
          <% if @posts.total_pages > 1 %>
            <div class="d-flex justify-content-center mt-4">
              <%= paginate @posts, 
                  previous_label: '← 前へ',
                  next_label: '次へ →' %>
            </div>
          <% end %>
        <% else %>
          <div class="card border-0 shadow-lg" style="background: white;">
            <div class="card-body text-center py-5">
              <div class="mb-3" style="font-size: 5rem;">📭</div>
              <h3 class="text-muted mb-3">まだ投稿がありません</h3>
              <p class="text-muted mb-4">最初の投稿を作成してみましょう！</p>
              <%= link_to root_path, class: "btn btn-primary btn-lg rounded-pill px-4" do %>
                <i class="bi bi-plus-circle"></i> 新規投稿
              <% end %>
            </div>
          </div>
        <% end %>
      </div>

      <!-- ポイント履歴タブ -->
      <div class="tab-pane fade" id="points" role="tabpanel">
        <div class="card border-0 shadow-lg" style="background: white;">
          <div class="card-body">
            <h5 class="card-title mb-4">
              <i class="bi bi-star-fill text-warning"></i>
              ポイント獲得履歴
            </h5>
            
            <!-- 今日と今週の獲得ポイント -->
            <div class="row mb-4">
              <div class="col-md-6">
                <div class="alert alert-info">
                  <strong>今日の獲得ポイント:</strong> +<%= @user.points_today %>pt
                </div>
              </div>
              <div class="col-md-6">
                <div class="alert alert-success">
                  <strong>今週の獲得ポイント:</strong> +<%= @user.points_this_week %>pt
                </div>
              </div>
            </div>
            
            <!-- ポイント履歴リスト -->
            <div class="list-group">
              <% @user.points.recent.limit(20).each do |point| %>
                <div class="list-group-item d-flex justify-content-between align-items-center">
                  <div>
                    <strong><%= point.action_type_name %></strong>
                    <br>
                    <small class="text-muted">
                      <%= point.created_at.strftime('%Y/%m/%d %H:%M') %>
                    </small>
                    <% if point.description %>
                      <br>
                      <small><%= point.description %></small>
                    <% end %>
                  </div>
                  <span class="badge bg-success rounded-pill" style="font-size: 1.1rem;">
                    +<%= point.amount %>pt
                  </span>
                </div>
              <% end %>
            </div>
            
            <% if @user.points.none? %>
              <p class="text-muted text-center mt-3">
                まだポイントを獲得していません
              </p>
            <% end %>
          </div>
        </div>
      </div>

      <!-- ミッションタブ -->
      <div class="tab-pane fade" id="missions" role="tabpanel">
        <div class="row">
          <!-- デイリーミッション -->
          <div class="col-md-6 mb-4">
            <div class="card border-0 shadow-lg">
              <div class="card-header text-white" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                <h5 class="mb-0">
                  <i class="bi bi-calendar-check"></i>
                  デイリーミッション
                </h5>
                <small>毎日23:59にリセット</small>
              </div>
              <div class="card-body">
                <% @user.todays_missions.each do |mission| %>
                  <div class="mb-3">
                    <div class="d-flex justify-content-between align-items-center mb-1">
                      <span>
                        <% if mission.completed? %>
                          <i class="bi bi-check-circle-fill text-success"></i>
                        <% else %>
                          <i class="bi bi-circle text-secondary"></i>
                        <% end %>
                        <%= mission.mission_name %>
                      </span>
                      <span class="badge bg-<%= mission.completed? ? 'success' : 'secondary' %>">
                        +<%= mission.reward_points %>pt
                      </span>
                    </div>
                    
                    <!-- 進捗バー -->
                    <div class="progress" style="height: 20px;">
                      <div class="progress-bar <%= mission.completed? ? 'bg-success' : '' %>" 
                           role="progressbar" 
                           style="width: <%= mission.progress_percentage %>%"
                           aria-valuenow="<%= mission.progress %>" 
                           aria-valuemin="0" 
                           aria-valuemax="<%= mission.target %>">
                        <%= mission.progress %> / <%= mission.target %>
                      </div>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
          
          <!-- ウィークリーミッション -->
          <div class="col-md-6 mb-4">
            <div class="card border-0 shadow-lg">
              <div class="card-header bg-success text-white">
                <h5 class="mb-0">
                  <i class="bi bi-trophy"></i>
                  ウィークリーミッション
                </h5>
                <small>毎週日曜23:59にリセット</small>
              </div>
              <div class="card-body">
                <% @user.current_week_missions.each do |mission| %>
                  <div class="mb-3">
                    <div class="d-flex justify-content-between align-items-center mb-1">
                      <span>
                        <% if mission.completed? %>
                          <i class="bi bi-check-circle-fill text-success"></i>
                        <% else %>
                          <i class="bi bi-circle text-secondary"></i>
                        <% end %>
                        <%= mission.mission_name %>
                      </span>
                      <span class="badge bg-<%= mission.completed? ? 'success' : 'secondary' %>">
                        +<%= mission.reward_points %>pt
                      </span>
                    </div>
                    
                    <!-- 進捗バー -->
                    <div class="progress" style="height: 20px;">
                      <div class="progress-bar bg-success" 
                           role="progressbar" 
                           style="width: <%= mission.progress_percentage %>%"
                           aria-valuenow="<%= mission.progress %>" 
                           aria-valuemin="0" 
                           aria-valuemax="<%= mission.target %>">
                        <%= mission.progress %> / <%= mission.target %>
                      </div>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- よく使うタグタブ -->
      <div class="tab-pane fade" id="tags" role="tabpanel">
        <div class="card border-0 shadow-lg" style="background: white;">
          <div class="card-body py-5">
            <h5 class="mb-4 text-center">
              <i class="bi bi-tags-fill text-primary"></i>
              よく使うタグ Top 5
            </h5>
            <% if @top_tags.any? %>
              <div class="d-flex flex-wrap justify-content-center gap-3">
                <% @top_tags.each do |tag, count| %>
                  <span class="badge rounded-pill" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); font-size: 1.2rem; padding: 0.8rem 1.5rem;">
                    #<%= tag %> (<%= count %>)
                  </span>
                <% end %>
              </div>
            <% else %>
              <p class="text-muted text-center mb-0">まだタグがありません</p>
            <% end %>
          </div>
        </div>
      </div>

      <!-- いいねした投稿タブ -->
      <div class="tab-pane fade" id="liked" role="tabpanel">
        <% if @liked_posts.any? %>
          <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            <% @liked_posts.each do |post| %>
              <div class="col">
                <%= link_to [post.genre, post], class: "text-decoration-none" do %>
                  <div class="card border-0 shadow-lg h-100 hover-card" style="background: white;">
                    <% if post.image.attached? %>
                      <%= image_tag url_for(post.image), 
                          class: "card-img-top", 
                          style: "height: 200px; object-fit: cover;" %>
                    <% else %>
                      <div class="card-img-top bg-light d-flex align-items-center justify-content-center" 
                           style="height: 200px;">
                        <span style="font-size: 4rem;">📚</span>
                      </div>
                    <% end %>

                    <div class="card-body">
                      <span class="badge bg-primary mb-2"><%= post.genre.name %></span>
                      <h5 class="card-title text-dark text-truncate"><%= post.manga_title %></h5>
                      <p class="card-text text-muted small">
                        <%= truncate(post.content, length: 80) %>
                      </p>

                      <div class="d-flex justify-content-between align-items-center">
                        <small class="text-muted">
                          <i class="bi bi-person"></i> <%= post.user.display_name %>
                        </small>
                        <span class="badge bg-danger">❤️ <%= post.likes_count %></span>
                      </div>
                    </div>
                  </div>
                <% end %>
              </div>
            <% end %>
          </div>

          <% if @liked_posts.count == 5 %>
            <div class="text-center mt-4">
              <small class="text-white-50">※ 最新5件のみ表示</small>
            </div>
          <% end %>
        <% else %>
          <div class="card border-0 shadow-lg" style="background: white;">
            <div class="card-body text-center py-5">
              <div class="mb-3" style="font-size: 5rem;">❤️</div>
              <h3 class="text-muted mb-3">まだいいねした投稿がありません</h3>
              <p class="text-muted mb-4">気に入った投稿にいいねしてみましょう！</p>
              <%= link_to root_path, class: "btn btn-primary btn-lg rounded-pill px-4" do %>
                <i class="bi bi-search"></i> 投稿を探す
              <% end %>
            </div>
          </div>
        <% end %>
      </div>

      <!-- 測定タブ -->
      <div class="tab-pane fade" id="measurements" role="tabpanel">
        <!-- グリット測定 -->
        <div class="card shadow-sm mb-4" style="background: white;">
          <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-3">
              <h5 class="mb-0">
                <i class="bi bi-clipboard2-check text-primary"></i>
                グリット測定
              </h5>
              <% if @user.latest_grit_score %>
                <span class="badge bg-success">測定済み</span>
              <% else %>
                <span class="badge bg-warning text-dark">未測定</span>
              <% end %>
            </div>

            <% if @user.latest_grit_score %>
              <% grit_score = @user.latest_grit_score %>
              
              <!-- スコア表示 -->
              <div class="row text-center mb-3">
                <div class="col-md-4">
                  <div class="p-3 bg-light rounded">
                    <div class="fs-2 fw-bold text-primary">
                      <%= grit_score.total_score %>
                    </div>
                    <div class="small text-muted">総合スコア / 4.00</div>
                  </div>
                </div>
                <div class="col-md-4">
                  <div class="p-3 bg-light rounded">
                    <div class="fs-4 fw-bold text-warning">
                      <%= grit_score.consistency_score %>
                    </div>
                    <div class="small text-muted">興味の一貫性</div>
                  </div>
                </div>
                <div class="col-md-4">
                  <div class="p-3 bg-light rounded">
                    <div class="fs-4 fw-bold text-danger">
                      <%= grit_score.perseverance_score %>
                    </div>
                    <div class="small text-muted">努力の粘り強さ</div>
                  </div>
                </div>
              </div>

              <!-- プログレスバー -->
              <div class="progress mb-3" style="height: 20px;">
                <div class="progress-bar" 
                     role="progressbar" 
                     style="width: <%= (grit_score.total_score / 4.0 * 100).round %>%; background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);"
                     aria-valuenow="<%= grit_score.total_score %>">
                  <%= (grit_score.total_score / 4.0 * 100).round %>%
                </div>
              </div>

              <p class="small text-muted mb-3">
                最終測定日: <%= grit_score.created_at.strftime('%Y年%m月%d日') %>
                (<%= grit_score.measurement_type_name %>)
              </p>

              <!-- 再測定ボタン -->
              <% if @user.pre_measurement_completed? && !@user.post_measurement_completed? %>
                <%= link_to new_grit_measurements_path(type: :post), class: "btn btn-primary" do %>
                  <i class="bi bi-arrow-repeat"></i> 事後測定を受ける
                <% end %>
              <% elsif @user.post_measurement_completed? %>
                <button class="btn btn-secondary" disabled>
                  <i class="bi bi-check-circle"></i> 測定完了
                </button>
              <% end %>

            <% else %>
              <!-- 未測定の場合 -->
              <p class="text-muted mb-3">
                まだグリット測定を受けていません。あなたの「やり抜く力」を測定してみましょう！
              </p>
              <%= link_to new_grit_measurements_path(type: :pre), class: "btn btn-primary" do %>
                <i class="bi bi-play-circle"></i> 事前測定を受ける
              <% end %>
            <% end %>
          </div>
        </div>

        <!-- ナラティブ測定 -->
        <div class="card shadow-sm mb-4" style="background: white;">
          <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-3">
              <h5 class="mb-0">
                <i class="bi bi-book-half text-success"></i>
                物語への移入度測定
              </h5>
              <% if @user.latest_narrative_score %>
                <span class="badge bg-success">測定済み</span>
              <% else %>
                <span class="badge bg-warning text-dark">未測定</span>
              <% end %>
            </div>

            <% if @user.latest_narrative_score %>
              <% narrative_score = @user.latest_narrative_score %>
              
              <!-- スコア表示 -->
              <div class="text-center mb-3">
                <div class="p-4 bg-light rounded">
                  <div class="fs-1 fw-bold text-success">
                    <%= narrative_score.score %>
                  </div>
                  <div class="small text-muted">移入度スコア / 7.00</div>
                </div>
              </div>

              <!-- プログレスバー -->
              <div class="progress mb-3" style="height: 20px;">
                <div class="progress-bar bg-success" 
                     role="progressbar" 
                     style="width: <%= (narrative_score.score / 7.0 * 100).round %>%;"
                     aria-valuenow="<%= narrative_score.score %>">
                  <%= (narrative_score.score / 7.0 * 100).round %>%
                </div>
              </div>

              <p class="small text-muted mb-3">
                最終測定日: <%= narrative_score.created_at.strftime('%Y年%m月%d日') %>
                (<%= narrative_score.measurement_type_name %>)
              </p>

              <!-- 再測定ボタン -->
              <% if @user.pre_measurement_completed? && !@user.post_measurement_completed? %>
                <%= link_to new_narrative_measurements_path(type: :post), class: "btn btn-success" do %>
                  <i class="bi bi-arrow-repeat"></i> 事後測定を受ける
                <% end %>
              <% elsif @user.post_measurement_completed? %>
                <button class="btn btn-secondary" disabled>
                  <i class="bi bi-check-circle"></i> 測定完了
                </button>
              <% end %>

            <% else %>
              <!-- 未測定の場合 -->
              <p class="text-muted mb-3">
                まだ物語への移入度測定を受けていません。マンガの物語世界への入り込み度を測定してみましょう！
              </p>
              <%= link_to new_narrative_measurements_path(type: :pre), class: "btn btn-success" do %>
                <i class="bi bi-play-circle"></i> 事前測定を受ける
              <% end %>
            <% end %>
          </div>
        </div>

        <!-- 測定の説明 -->
        <div class="card shadow-sm border-info" style="background: white;">
          <div class="card-body">
            <h6 class="mb-3">
              <i class="bi bi-info-circle text-info"></i>
              測定について
            </h6>
            <ul class="small mb-0">
              <li class="mb-2"><strong>事前測定:</strong> プラットフォーム利用開始時に実施</li>
              <li class="mb-2"><strong>事後測定:</strong> 1ヶ月後に実施</li>
              <li>各測定は1回のみ受けられます</li>
            </ul>
          </div>
        </div>
        <!-- スコア比較（事後測定完了後のみ表示） -->
        <% if @user.post_measurement_completed? %>
          <div class="card shadow-sm mt-4" style="background: white;">
            <div class="card-body">
              <h5 class="mb-4">
                <i class="bi bi-graph-up-arrow text-primary"></i>
                スコア推移
              </h5>

              <!-- グリットスコア推移 -->
              <div class="mb-4">
                <h6 class="mb-3">
                  <i class="bi bi-clipboard2-check text-primary"></i>
                  グリットスコア（やり抜く力）
                </h6>
                
                <div class="row text-center">
                  <!-- 事前 -->
                  <div class="col-md-4">
                    <div class="p-3 bg-light rounded">
                      <div class="small text-muted mb-1">事前測定</div>
                      <div class="fs-3 fw-bold text-primary"><%= @pre_grit_score.total_score %></div>
                      <div class="small text-muted">
                        <%= @pre_grit_score.created_at.strftime('%m/%d') %>
                      </div>
                    </div>
                  </div>

                  <!-- 矢印 -->
                  <div class="col-md-4 d-flex align-items-center justify-content-center">
                    <% change = @post_grit_score.total_score - @pre_grit_score.total_score %>
                    <% if change > 0 %>
                      <div class="text-center">
                        <i class="bi bi-arrow-up-circle-fill fs-1 text-success"></i>
                        <div class="fs-4 fw-bold text-success">+<%= change.round(2) %></div>
                        <div class="small text-muted">向上しました！</div>
                      </div>
                    <% elsif change < 0 %>
                      <div class="text-center">
                        <i class="bi bi-arrow-down-circle-fill fs-1 text-warning"></i>
                        <div class="fs-4 fw-bold text-warning"><%= change.round(2) %></div>
                        <div class="small text-muted">変化</div>
                      </div>
                    <% else %>
                      <div class="text-center">
                        <i class="bi bi-dash-circle-fill fs-1 text-secondary"></i>
                        <div class="fs-4 fw-bold text-secondary">0.00</div>
                        <div class="small text-muted">変化なし</div>
                      </div>
                    <% end %>
                  </div>

                  <!-- 事後 -->
                  <div class="col-md-4">
                    <div class="p-3 bg-light rounded">
                      <div class="small text-muted mb-1">事後測定</div>
                      <div class="fs-3 fw-bold text-primary"><%= @post_grit_score.total_score %></div>
                      <div class="small text-muted">
                        <%= @post_grit_score.created_at.strftime('%m/%d') %>
                      </div>
                    </div>
                  </div>
                </div>

                <!-- 詳細スコア比較 -->
                <div class="row mt-3 text-center small">
                  <div class="col-6">
                    <div class="p-2 bg-light rounded">
                      <div class="mb-1 text-muted">興味の一貫性</div>
                      <div class="fw-bold">
                        <%= @pre_grit_score.consistency_score %> → <%= @post_grit_score.consistency_score %>
                        <% consistency_change = @post_grit_score.consistency_score - @pre_grit_score.consistency_score %>
                        <span class="<%= consistency_change >= 0 ? 'text-success' : 'text-warning' %>">
                          (<%= consistency_change >= 0 ? '+' : '' %><%= consistency_change.round(2) %>)
                        </span>
                      </div>
                    </div>
                  </div>
                  <div class="col-6">
                    <div class="p-2 bg-light rounded">
                      <div class="mb-1 text-muted">努力の粘り強さ</div>
                      <div class="fw-bold">
                        <%= @pre_grit_score.perseverance_score %> → <%= @post_grit_score.perseverance_score %>
                        <% perseverance_change = @post_grit_score.perseverance_score - @pre_grit_score.perseverance_score %>
                        <span class="<%= perseverance_change >= 0 ? 'text-success' : 'text-warning' %>">
                          (<%= perseverance_change >= 0 ? '+' : '' %><%= perseverance_change.round(2) %>)
                        </span>
                      </div>
                    </div>
                  </div>
                </div>
              </div>

              <hr>

              <!-- ナラティブスコア推移 -->
              <div>
                <h6 class="mb-3">
                  <i class="bi bi-book-half text-success"></i>
                  物語への移入度
                </h6>
                
                <div class="row text-center">
                  <!-- 事前 -->
                  <div class="col-md-4">
                    <div class="p-3 bg-light rounded">
                      <div class="small text-muted mb-1">事前測定</div>
                      <div class="fs-3 fw-bold text-success"><%= @pre_narrative_score.score %></div>
                      <div class="small text-muted">
                        <%= @pre_narrative_score.created_at.strftime('%m/%d') %>
                      </div>
                    </div>
                  </div>

                  <!-- 矢印 -->
                  <div class="col-md-4 d-flex align-items-center justify-content-center">
                    <% narrative_change = @post_narrative_score.score - @pre_narrative_score.score %>
                    <% if narrative_change > 0 %>
                      <div class="text-center">
                        <i class="bi bi-arrow-up-circle-fill fs-1 text-success"></i>
                        <div class="fs-4 fw-bold text-success">+<%= narrative_change.round(2) %></div>
                        <div class="small text-muted">向上しました！</div>
                      </div>
                    <% elsif narrative_change < 0 %>
                      <div class="text-center">
                        <i class="bi bi-arrow-down-circle-fill fs-1 text-warning"></i>
                        <div class="fs-4 fw-bold text-warning"><%= narrative_change.round(2) %></div>
                        <div class="small text-muted">変化</div>
                      </div>
                    <% else %>
                      <div class="text-center">
                        <i class="bi bi-dash-circle-fill fs-1 text-secondary"></i>
                        <div class="fs-4 fw-bold text-secondary">0.00</div>
                        <div class="small text-muted">変化なし</div>
                      </div>
                    <% end %>
                  </div>

                  <!-- 事後 -->
                  <div class="col-md-4">
                    <div class="p-3 bg-light rounded">
                      <div class="small text-muted mb-1">事後測定</div>
                      <div class="fs-3 fw-bold text-success"><%= @post_narrative_score.score %></div>
                      <div class="small text-muted">
                        <%= @post_narrative_score.created_at.strftime('%m/%d') %>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

  <style>
    .hover-card {
      transition: all 0.3s ease;
    }
    .hover-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 1rem 3rem rgba(0,0,0,0.2) !important;
    }

    .hover-stat-card {
      transition: all 0.3s ease;
    }
    .hover-stat-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 1rem 2rem rgba(0,0,0,0.15) !important;
    }

    .nav-pills .nav-link.active {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }
    .nav-pills .nav-link {
      color: #667eea;
      font-weight: 600;
      transition: all 0.2s;
    }
    .nav-pills .nav-link:hover:not(.active) {
      background-color: #f8f9fa;
    }

    /* ページネーションのスタイル */
    .pagination {
      background: white;
      padding: 1rem 1.5rem;
      border-radius: 1rem;
      box-shadow: 0 0.5rem 1rem rgba(0,0,0,0.15);
      margin: 0;
      display: inline-flex;
      gap: 0.5rem;
    }
    
    .pagination .page-link {
      color: #667eea;
      border: none;
      padding: 0.75rem 1.25rem;
      margin: 0;
      border-radius: 0.5rem;
      font-weight: 600;
      transition: all 0.2s;
      font-size: 1.1rem;
      min-width: 3rem; 
    }
    
    .pagination .page-link:hover {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      transform: translateY(-2px);
    }
    
    .pagination .page-item.active .page-link {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
    }
    
    .pagination .page-item.disabled .page-link {
      color: #ccc;
      background: transparent;
    }
  </style>
</body>
</html>