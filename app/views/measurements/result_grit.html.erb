<!DOCTYPE html>
<html>
<head>
  <title>グリット測定結果 - マンガグリッドプラットフォーム</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <style>
    .result-card {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }
    .score-display {
      font-size: 4rem;
      font-weight: bold;
    }
    .sub-score {
      font-size: 2rem;
    }
  </style>
</head>
<body class="bg-light">
  <div class="container">
    <div class="row justify-content-center mt-5">
      <div class="col-md-10">
        <!-- 完了アニメーション -->
        <div class="text-center mb-4">
          <i class="bi bi-check-circle-fill text-success" style="font-size: 5rem;"></i>
          <h2 class="mt-3 fw-bold">測定完了！</h2>
        </div>

        <!-- 結果カード -->
        <div class="card shadow-lg border-0 result-card mb-4">
          <div class="card-body p-5 text-center">
            <h3 class="mb-4">総合グリットスコア</h3>
            
            <div class="score-display mb-3">
              <%= @grit_score.total_score %>
            </div>
            
            <div class="mb-4">
              <span class="fs-5">/ 4.00</span>
            </div>

            <!-- プログレスバー -->
            <div class="progress mb-4" style="height: 30px; background-color: rgba(255,255,255,0.3);">
              <div class="progress-bar" 
                   role="progressbar" 
                   style="width: <%= (@grit_score.total_score / 4.0 * 100).round %>%; background-color: rgba(255,255,255,0.9);"
                   aria-valuenow="<%= @grit_score.total_score %>" 
                   aria-valuemin="0" 
                   aria-valuemax="4">
                <strong style="color: #667eea;">
                  <%= (@grit_score.total_score / 4.0 * 100).round %>%
                </strong>
              </div>
            </div>
          </div>
        </div>

        <!-- 詳細スコア -->
        <div class="row mb-4">
          <div class="col-md-6 mb-3">
            <div class="card shadow border-0 h-100">
              <div class="card-body text-center p-4">
                <i class="bi bi-lightbulb text-warning fs-1 mb-3"></i>
                <h5 class="mb-3">興味の一貫性</h5>
                <div class="sub-score text-primary">
                  <%= @grit_score.consistency_score %>
                </div>
                <div class="text-muted mb-3">/ 4.00</div>
                <div class="progress" style="height: 10px;">
                  <div class="progress-bar bg-warning" 
                       role="progressbar" 
                       style="width: <%= (@grit_score.consistency_score / 4.0 * 100).round %>%">
                  </div>
                </div>
                <p class="small text-muted mt-3 mb-0">
                  <%= @grit_score.consistency_evaluation %>
                </p>
              </div>
            </div>
          </div>

          <div class="col-md-6 mb-3">
            <div class="card shadow border-0 h-100">
              <div class="card-body text-center p-4">
                <i class="bi bi-fire text-danger fs-1 mb-3"></i>
                <h5 class="mb-3">努力の粘り強さ</h5>
                <div class="sub-score text-primary">
                  <%= @grit_score.perseverance_score %>
                </div>
                <div class="text-muted mb-3">/ 4.00</div>
                <div class="progress" style="height: 10px;">
                  <div class="progress-bar bg-danger" 
                       role="progressbar" 
                       style="width: <%= (@grit_score.perseverance_score / 4.0 * 100).round %>%">
                  </div>
                </div>
                <p class="small text-muted mt-3 mb-0">
                  <%= @grit_score.perseverance_evaluation %>
                </p>
              </div>
            </div>
          </div>
        </div>

        <!-- 総合評価 -->
        <div class="card shadow border-0 mb-4">
          <div class="card-body p-4">
            <h5 class="mb-3">
              <i class="bi bi-chat-square-text text-primary me-2"></i>
              総合評価
            </h5>
            <p class="mb-0" style="line-height: 1.8;">
              <%= @grit_score.evaluation_comment %>
            </p>
          </div>
        </div>

        <!-- 次のステップ -->
        <div class="card shadow border-0 mb-4" style="border-left: 4px solid #667eea !important;">
          <div class="card-body p-4">
            <h5 class="mb-3">
              <i class="bi bi-arrow-right-circle text-primary me-2"></i>
              次のステップ
            </h5>
            <ul class="mb-0">
              <li class="mb-2">物語への移入度測定を受けましょう</li>
              <li class="mb-2">プラットフォームで積極的に投稿・コメントしましょう</li>
              <li class="mb-2">1ヶ月後に再測定してスコアの変化を確認しましょう</li>
            </ul>
          </div>
        </div>

        <!-- ボタン -->
        <div class="d-flex gap-3 justify-content-center mb-5">
          <%= link_to new_narrative_measurements_path(type: @measurement_type), 
              class: "btn btn-success btn-lg" do %>
            <i class="bi bi-book me-2"></i>
            物語への移入度測定へ
          <% end %>

          <%= link_to mypage_path, 
              class: "btn btn-outline-primary btn-lg" do %>
            <i class="bi bi-person-circle me-2"></i>
            マイページに戻る
          <% end %>
        </div>

        <!-- 測定日 -->
        <div class="text-center text-muted small">
          <p class="mb-0">
            測定日時: <%= @grit_score.created_at.strftime('%Y年%m月%d日 %H:%M') %>
          </p>
          <p class="mb-0">
            測定タイプ: <%= @grit_score.measurement_type_name %>
          </p>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>