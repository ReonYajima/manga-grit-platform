<!DOCTYPE html>
<html>
<head>
  <title>マンガグリットプラットフォーム</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-4">
    <!-- ヘッダー部分 -->
    <div class="text-center mb-4">
      <h1 class="display-4">マンガグリットプラットフォーム</h1>
      
      <!-- ユーザー情報とログアウト -->
      <div class="text-end">
        <span class="me-3">ようこそ、<%= current_user.display_name %>さん</span>
        <%= link_to "ログアウト", destroy_user_session_path, 
            data: { turbo_method: :delete }, 
            class: "btn btn-outline-secondary btn-sm" %>
      </div>
    </div>

    <!-- ジャンル選択画面 -->
    <div class="text-center mb-4">
      <h2>ジャンル選択</h2>
      <p class="text-muted">ジャンルを選んでください</p>
    </div>

    <!-- ジャンルカード一覧 -->
    <div class="row">
      <% @genres.each do |genre| %>
        <div class="col-md-4 mb-4">
          <%= link_to genre_path(genre), class: "text-decoration-none" do %>
            <div class="card h-100 shadow-sm hover-card">
              <div class="card-body text-center">
                <h5 class="card-title text-primary"><%= genre.name %></h5>
                <p class="card-text text-muted small"><%= genre.description %></p>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  
  <style>
    .hover-card:hover {
      transform: translateY(-2px);
      transition: transform 0.2s;
    }
  </style>
</body>
</html>