<!DOCTYPE html>
<html>
<head>
  <title><%= @genre.name %> - マンガグリットプラットフォーム</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-4">
    <!-- ヘッダー部分 -->
    <div class="d-flex justify-content-between align-items-center mb-4">
      <div>
        <h1 class="h2"><%= @genre.name %></h1>
        <p class="text-muted"><%= @genre.description %></p>
      </div>
      <div>
        <%= link_to "← 成長テーマ選択に戻る", root_path, class: "btn btn-outline-secondary" %>
      </div>
    </div>

    <!-- 新規投稿ボタン -->
    <div class="text-center mb-4">
      <%= link_to "新規投稿", new_genre_post_path(@genre), class: "btn btn-primary btn-lg" %>
    </div>

    <!-- 投稿一覧 -->
    <div class="row">
      <% if @posts.any? %>
        <% @posts.each do |post| %>
          <div class="col-md-6 col-lg-4 mb-4">
            <%= link_to [@genre, post], class: "text-decoration-none" do %>
              <div class="card h-100 shadow-sm hover-card">
                <!-- 画像サムネイル -->
                <% if post.image.attached? %>
                  <%= image_tag url_for(post.image), 
                      class: "card-img-top", 
                      style: "height: 200px; object-fit: cover;" %>
                <% else %>
                  <div class="card-img-top bg-light d-flex align-items-center justify-content-center" 
                       style="height: 200px;">
                    <span class="text-muted">📚 画像なし</span>
                  </div>
                <% end %>

                <div class="card-body">
                  <h5 class="card-title text-primary"><%= post.manga_title %></h5>
                  <p class="card-text text-muted small mb-2">
                    <%= truncate(post.content, length: 100) %>
                  </p>
                  
                  <!-- タグ表示 -->
                  <% if post.tag_list.any? %>
                    <div class="mb-2">
                      <% post.tag_list.first(3).each do |tag| %>
                        <span class="badge bg-secondary me-1">#<%= tag %></span>
                      <% end %>
                      <% if post.tag_list.count > 3 %>
                        <span class="badge bg-secondary">+<%= post.tag_list.count - 3 %></span>
                      <% end %>
                    </div>
                  <% end %>
                  
                  <!-- 投稿者情報 -->
                  <div class="d-flex justify-content-between align-items-center">
                    <small class="text-muted">by <%= post.user.display_name %></small>
                    <small class="text-muted">
                      ❤️ <%= post.likes_count %> 
                      💬 <%= post.comments.count %>
                    </small>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>
      <% else %>
        <!-- 投稿がない場合 -->
        <div class="col-12 text-center">
          <div class="card">
            <div class="card-body py-5">
              <h5 class="card-title text-muted">まだ投稿がありません</h5>
              <p class="card-text">最初の投稿を作成してみましょう！</p>
              <%= link_to "新規投稿", new_genre_post_path(@genre), class: "btn btn-primary" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <!-- ページネーション -->
    <% if @posts.respond_to?(:current_page) %>
      <div class="d-flex justify-content-center mt-4">
        <%= paginate @posts %>
      </div>
    <% end %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  
  <style>
    .hover-card {
      transition: transform 0.2s, box-shadow 0.2s;
    }
    .hover-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 0.5rem 1rem rgba(0,0,0,0.15) !important;
    }
  </style>
</body>
</html>