<!DOCTYPE html>
<html>
<head>
  <title><%= @post.manga_title %> - マンガグリットプラットフォーム</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <div>
        <%= link_to "← #{@post.genre.name}に戻る", @post.genre, class: "btn btn-outline-secondary" %>
      </div>
      <div>
        <% if @post.user == current_user %>
          <%= link_to "編集", edit_genre_post_path(@post.genre, @post), class: "btn btn-outline-primary me-2" %>
          <%= link_to "削除", [@post.genre, @post], 
              data: { 
                turbo_method: :delete, 
                turbo_confirm: "本当に削除しますか?" 
              }, 
              class: "btn btn-outline-danger" %>
        <% end %>
      </div>
    </div>

    <div class="row">
      <div class="col-lg-8">
        <div class="card">
          <div class="card-body">
            <h1 class="card-title text-primary mb-3"><%= @post.manga_title %></h1>
            
            <div class="d-flex justify-content-between align-items-center mb-3">
              <div class="d-flex align-items-center">
                <strong class="me-3">by <%= @post.user.display_name %></strong>
                <small class="text-muted"><%= @post.created_at.strftime("%Y年%m月%d日 %H:%M") %></small>
              </div>
            </div>

            <% if @post.tag_list.any? %>
              <div class="mb-3">
                <% @post.tag_list.each do |tag| %>
                  <span class="badge bg-secondary me-1">#<%= tag %></span>
                <% end %>
              </div>
            <% end %>

            <div class="card-text">
              <p style="white-space: pre-wrap; line-height: 1.8;"><%= @post.content %></p>
            </div>
          </div>
        </div>

        <div class="card mt-3">
          <div class="card-body text-center">
            <% if @post.liked_by?(current_user) %>
              <%= link_to post_like_path(@post, @post.likes.find_by(user: current_user)), 
                  data: { turbo_method: :delete }, class: "btn btn-danger" do %>
                ❤️ いいね済み (<%= @post.likes_count %>)
              <% end %>
            <% else %>
              <%= link_to post_likes_path(@post), data: { turbo_method: :post }, class: "btn btn-outline-danger" do %>
                🤍 いいね (<%= @post.likes_count %>)
              <% end %>
            <% end %>
          </div>
        </div>
      </div>

      <div class="col-lg-4">
        <div class="card">
          <div class="card-header">
            <h5 class="mb-0">コメント (<%= @comments.count %>)</h5>
          </div>
          <div class="card-body">
            <% if @comments.any? %>
              <% @comments.each do |comment| %>
                <div class="mb-3 pb-3 border-bottom">
                  <div class="d-flex justify-content-between align-items-start mb-2">
                    <strong><%= comment.user.display_name %></strong>
                    <small class="text-muted"><%= comment.created_at.strftime("%m/%d %H:%M") %></small>
                  </div>
                  <p class="mb-1" style="white-space: pre-wrap;"><%= comment.content %></p>
                  <% if comment.user == current_user %>
                    <%= link_to "削除", [@post, comment], 
                        data: { 
                          turbo_method: :delete, 
                          turbo_confirm: "本当に削除しますか?" 
                        }, 
                        class: "btn btn-sm btn-outline-danger" %>
                  <% end %>
                </div>
              <% end %>
            <% else %>
              <p class="text-muted">まだコメントがありません</p>
            <% end %>

            <%= form_with(model: [@post, @comment], local: true) do |form| %>
              <div class="mt-3">
                <%= form.text_area :content, rows: 3, class: "form-control mb-2", 
                    placeholder: "コメントを入力..." %>
                <%= form.submit "コメント投稿", class: "btn btn-primary btn-sm" %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>