<!DOCTYPE html>
<html>
<head>
  <title><%= @post.manga_title %> - マンガグリットプラットフォーム</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <div>
        <%= link_to "← #{@post.genre.name}に戻る", @post.genre, class: "btn btn-outline-secondary" %>
      </div>
      <div>
        <% if @post.user == current_user %>
          <%= link_to "編集", edit_genre_post_path(@post.genre, @post), class: "btn btn-outline-primary me-2" %>
          <%= form_with url: [@post.genre, @post], method: :delete, local: true, style: "display: inline;" do %>
            <%= submit_tag "削除", data: { confirm: "本当に削除しますか?" }, class: "btn btn-outline-danger" %>
          <% end %>
        <% end %>
      </div>
    </div>

    <div class="row">
      <div class="col-md-8 mx-auto">
        <div class="card shadow mb-4">
          <div class="card-body">
            <h1 class="card-title h3 mb-3"><%= @post.manga_title %></h1>
            
            <div class="mb-3">
              <small class="text-muted">
                by <%= @post.user.display_name %>
                <span class="mx-2">|</span>
                <%= @post.created_at.strftime("%Y年%m月%d日 %H:%M") %>
              </small>
            </div>

            <% if @post.tag_list.any? %>
              <div class="mb-3">
                <% @post.tag_list.each do |tag| %>
                  <span class="badge bg-secondary">#<%= tag %></span>
                <% end %>
              </div>
            <% end %>

            <% if @post.image.attached? %>
              <div class="mb-4">
                <%= image_tag url_for(@post.image), 
                    class: "img-fluid rounded", 
                    style: "max-height: 600px; width: 100%; object-fit: contain; background-color: #f8f9fa;" %>
                
                <% if @post.manga_author.present? %>
                  <div class="mt-2 p-3 bg-light border rounded">
                    <small class="text-muted">
                      <strong>📖 出典：</strong>
                      <%= @post.formatted_citation %>
                    </small>
                  </div>
                <% end %>
              </div>
            <% end %>

            <div class="card-text" style="white-space: pre-wrap;"><%= @post.content %></div>
          </div>
        </div>

        <div class="mb-4">
          <% if @post.liked_by?(current_user) %>
            <%= form_with url: post_like_path(@post, @post.likes.find_by(user: current_user)), method: :delete, local: true, style: "display: inline;" do %>
              <%= submit_tag "❤️ いいね済み (#{@post.likes_count})", class: "btn btn-danger" %>
            <% end %>
          <% else %>
            <%= form_with url: post_likes_path(@post), method: :post, local: true, style: "display: inline;" do %>
              <%= submit_tag "🤍 いいね (#{@post.likes_count})", class: "btn btn-outline-danger" %>
            <% end %>
          <% end %>
        </div>

        <div class="card shadow">
          <div class="card-header bg-light">
            <h5 class="mb-0">コメント (<%= @comments.count %>)</h5>
          </div>
          <div class="card-body">
            <% if @comments.any? %>
              <% @comments.each do |comment| %>
                <div class="mb-3 pb-3 border-bottom">
                  <div class="d-flex justify-content-between align-items-start mb-2">
                    <small class="text-muted">
                      <strong><%= comment.user.display_name %></strong>
                      <span class="mx-2">|</span>
                      <%= comment.created_at.strftime("%m/%d %H:%M") %>
                    </small>
                    <% if comment.user == current_user %>
                      <%= form_with url: [@post, comment], method: :delete, local: true, style: "display: inline;" do %>
                        <%= submit_tag "削除", data: { confirm: "本当に削除しますか?" }, class: "btn btn-sm btn-outline-danger" %>
                      <% end %>
                    <% end %>
                  </div>
                  <p class="mb-0" style="white-space: pre-wrap;"><%= comment.content %></p>
                </div>
              <% end %>
            <% else %>
              <p class="text-muted mb-3">まだコメントがありません</p>
            <% end %>

            <%= form_with(model: [@post, @comment], local: true) do |form| %>
              <div class="mb-3">
                <%= form.text_area :content, rows: 3, class: "form-control",
                    placeholder: "コメントを入力..." %>
              </div>
              <%= form.submit "コメント投稿", class: "btn btn-primary" %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>